language: python
python:
  - "3.4"
  - "3.5"
  - "3.6"
  # PyPy versions
  - "pypy3"
# command to install dependencies
install: "pip install --user -r requirements.txt"
# command to run tests
script: pytest
